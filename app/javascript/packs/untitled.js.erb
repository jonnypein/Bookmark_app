.added


const bookmark = document.querySelector('.add-to-bookmarks')

console.log(bookmark);

// function changedToBookmark() {
//   bookmark.innerHTML = "Added to your Bookmark";
// };

bookmark.addEventListener("click", () => {
   bookmark.innerHTML = "Bookmarked!";
});

conts recommendation = document.querySelector('.add-to-recommendations')


function changedToRecommendation() {
 recommendation.innerHTML = "Recommended!";
};

recommendation.addEventListener("click", changedToRecommendation);
